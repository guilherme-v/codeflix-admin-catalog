package com.gsv.codeflix.admin.catalog.domain;

/**
 * Value Object: Objects that matter only as the combination of their attributes.
 * Two value objects with the same values for all their attributes are considered equal.
 * Also has no identity and are usually immutable
 */
public abstract class ValueObject {

}
